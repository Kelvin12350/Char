echo '{"appId":"com.yourname.mega","appName":"MEGA","webDir":"."}' > capacitor.config.json && mkdir -p .github/workflows && echo "name: Build MEGA APK\n\non:\n  push:\n    branches: [ main ]\n\njobs:\n  build:\n    name: Build Android\n    runs-on: ubuntu-latest\n\n    steps:\n    - name: Checkout repo\n      uses: actions/checkout@v3\n\n    - name: Setup Node.js\n      uses: actions/setup-node@v3\n      with:\n        node-version: '20'\n\n    - name: Install dependencies\n      run: npm install\n\n    - name: Add Android platform\n      run: npx cap add android\n\n    - name: Sync Capacitor\n      run: npx cap sync android\n\n    - name: Build APK\n      run: |\n        cd android\n        ./gradlew assembleDebug\n\n    - name: Upload APK artifact\n      uses: actions/upload-artifact@v4\n      with:\n        name: MEGA-debug.apk\n        path: android/app/build/outputs/apk/debug/app-debug.apk" > .github/workflows/build.yml && git add . && git commit -m "Fix config & workflow; trigger build" && git push origin main
